pipeline {
    // agent { docker { image 'python:3.10.7-alpine' } }
    agent any
    
    stages {
        stage('build') {
            steps {
                sh 'python --version'
            }
        }
    }
}